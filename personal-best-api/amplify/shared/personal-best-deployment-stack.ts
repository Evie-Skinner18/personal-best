import { Stack } from "aws-cdk-lib";

// to-do want to export the stack once from here so it's reuseable
// const personalBestDeploymentStack = new Stack('RdsStack');


// export default personalBestDeploymentStack;